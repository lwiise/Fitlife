# Copy this file to `apps/e2e/.env.e2e` and fill it in.
# (Named `.txt` here because the repo's deny list blocks writing dotfiles directly.)
#
# `.env.e2e` is gitignored. Values already present in the real environment always
# win, so CI can inject these as secrets without a file on disk.
#
# If you already have `apps/app/.env.local`, most of these are picked up from it
# automatically — in practice you usually only need E2E_ALLOW_TARGET (and only
# when you are NOT running against localhost).

# ---------- What is under test ----------

# The running Next.js app. Defaults to the dev server's port.
E2E_BASE_URL=http://localhost:3001

# Set to 1 to have Playwright start `pnpm --filter @fitlife/app dev` itself.
# Leave unset if you already have the app running in another terminal.
# E2E_MANAGE_WEBSERVER=1

# ---------- Safety ----------

# DEFAULT-DENY. Any non-local app or Supabase host must be listed here by exact
# hostname before the suite will write a single row. Comma-separated.
# Production (fitlife-app-mvp.netlify.app) is refused even if listed.
# E2E_ALLOW_TARGET=my-staging-branch.netlify.app,abcdefgh.supabase.co

# ---------- Supabase (the DISPOSABLE stack under test) ----------
# Falls back to NEXT_PUBLIC_SUPABASE_URL / _ANON_KEY / SUPABASE_SERVICE_ROLE_KEY
# from apps/app/.env.local.
#
# The service-role key is used to mint pre-confirmed test users and to hard-delete
# them afterwards. Never point this at a database with real customers.
E2E_SUPABASE_URL=
E2E_SUPABASE_ANON_KEY=
E2E_SUPABASE_SERVICE_ROLE_KEY=

# ---------- LemonSqueezy (TEST MODE ONLY) ----------

# Must MATCH the LEMONSQUEEZY_WEBHOOK_SECRET the app under test is running with —
# the suite signs its simulated payment events with it, and the app verifies them.
E2E_LEMONSQUEEZY_WEBHOOK_SECRET=

# Optional. When set, the suite also reads the variant back from the LemonSqueezy
# API so the billed amount is verified against the provider, not only against
# packages/config/src/pricing.ts. Use a TEST-MODE key.
# E2E_LEMONSQUEEZY_API_KEY=

# ---------- Optional behaviour ----------

# Also drive LemonSqueezy's hosted checkout page in a real browser with the test
# card 4242 4242 4242 4242. Off by default: it depends on a third-party page's
# markup. Requires the app to have LEMONSQUEEZY_API_KEY + LEMONSQUEEZY_STORE_ID.
# E2E_LIVE_CHECKOUT=1

# Keep created test accounts instead of deleting them, for debugging a failure.
# They are NOT leaked permanently: the next run's setup sweeps them.
# E2E_KEEP_ACCOUNTS=1
