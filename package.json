{
  "name": "fitlife",
  "version": "2.0.0",
  "private": true,
  "description": "Fit Life 2.0 — Smart nutrition for Gulf families",
  "scripts": {
    "dev": "turbo run dev",
    "dev:app": "turbo run dev --filter=@fitlife/app",
    "dev:landing": "turbo run dev --filter=@fitlife/landing",
    "build": "turbo run build",
    "build:app": "turbo run build --filter=@fitlife/app",
    "build:landing": "turbo run build --filter=@fitlife/landing",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@netlify/plugin-nextjs": "^5.15.13",
    "@types/node": "^22.0.0",
    "prettier": "^3.9.6",
    "supabase": "^2.110.0",
    "turbo": "^2.10.7",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
