{
  "name": "fitlife",
  "version": "2.0.0",
  "private": true,
  "description": "Fit Life 2.0 — Smart nutrition for Gulf families",
  "scripts": {
    "dev": "turbo run dev",
    "dev:app": "turbo run dev --filter=@fitlife/app",
    "build": "turbo run build",
    "build:app": "turbo run build --filter=@fitlife/app",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@netlify/plugin-nextjs": "^5.15.12",
    "@types/node": "^22.20.1",
    "prettier": "^3.9.6",
    "supabase": "^2.109.1",
    "turbo": "^2.10.7",
    "typescript": "^5.6.0"
  },
  "pnpm": {
    "overrides": {
      "dompurify@<3.4.12": "^3.4.12"
    },
    "overridesComment": "posthog-js 1.407.2 (latest) still pins dompurify 3.4.4, which carries a high-severity XSS advisory plus seven more. 3.4.12 is the same patch line, so this only moves the transitive pin forward until posthog-js updates its own. Drop this entry once it does."
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
